const ProfileDescripter = ({ description }: { description: string }) => {
  return (
    <div className="">
      <p className="text-sm text-black">{description}</p>
    </div>
  );
};

export default ProfileDescripter;
